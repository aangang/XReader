<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 背景图 -->
    <item android:id="@+id/background" android:drawable="@drawable/seekbar_second_progress" />
    <!-- 第二进度图 -->
    <item android:id="@+id/SecondaryProgress" android:drawable="@drawable/seekbar_second_progress" />
    <!-- 进度度 -->
    <item android:id="@+id/progress" android:drawable="@drawable/seekbar_progress" />
</layer-list>